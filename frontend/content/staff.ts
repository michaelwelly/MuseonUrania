// Прямые контакты сотрудников.
//
// Здесь были шесть человек с именами, должностями, телефонами и адресами —
// заглушки из макета (design_handoff_vedal_redesign/README.md → Fidelity).
// Они стояли на четырёх публичных страницах: контакты, документы, новости
// и сервис. Примечания «это демо-данные» рядом с ними было мало: телефон
// в разметке выглядит как телефон, по нему звонят, и звонок уходит в никуда.
//
// Убраны 18 августа 2026 по §9.2 плана заказчика. Взамен показывается
// единственный подтверждённый канал компании — номер с бланка датащитов
// (docs/products/README.md).
//
// Что нужно от заказчика, чтобы вернуть блок (§9.3 плана): ФИО, должность,
// email, телефон и — отдельно — разрешение публиковать каждый из контактов.
//
// Названия отделов здесь тоже не выдумываются. «Сервисная служба» и
// «пресс-служба» звучат безобидно, но это утверждение о структуре компании,
// а её нам никто не подтверждал.

import { AWAITING, site } from "./site";

export type Contact = {
  title: string;
  scope: string;
  phone: string;
  email: string;
};

export const STAFF_AWAITING = `Прямые контакты ответственных сотрудников — ${AWAITING}. Обращение по общему номеру попадёт в нужный отдел.`;

export const companyContact: Contact = {
  title: site.legalName,
  scope: "Подбор оборудования, документы, сервис и партнёрство",
  phone: site.phone,
  email: site.email,
};
