// Страница «Новости и пресс-центр» (экран 08 редизайна).
//
// Публикаций нет: материалы Иннопрома, разрешённые фотографии и пресс-контакт
// числятся в «Awaiting NN» и в docs/requests/nikolay_materials_request.md.
// Демо-публикации из макета в прод не переносим — прямое указание хендоффа.
// Массив пустой, страница показывает пустое состояние; вёрстка ленты готова
// и включится сама, как только появится первая запись.

import { AWAITING, site } from "./site";

// «Пресс-служба» отсюда убрана. Строкой ниже страница сама писала, что
// пресс-контакт ожидает уточнения, — и тут же отправляла журналиста в отдел,
// существование которого никто не подтверждал. То же правило записано в
// content/staff.ts: название отдела — это утверждение о структуре компании,
// а не безобидная формулировка. Пока структуру не подтвердили, работает
// единственный подтверждённый канал — номер с бланка датащитов и общая почта.
export const newsHero = {
  title: "Новости и пресс-центр",
  lead: "Выставки, обновления изделий, производство и документы. Запросы на комментарии и материалы принимаем по общему номеру и почте.",
};

export const tags = ["Продукция", "Производство", "Выставки", "Сервис", "Документы"] as const;
export type Tag = (typeof tags)[number];

export type NewsItem = {
  date: string;
  tag: Tag;
  title: string;
  excerpt: string;
  image?: { src: string; alt: string };
};

/** Пусто до передачи материалов. Первой записью планируется релиз по Иннопрому. */
export const news: NewsItem[] = [];

export const expected = [
  `Пресс-релиз и материалы с Иннопрома — ${AWAITING}.`,
  `Фотографии со стенда, разрешённые к публикации, — ${AWAITING}.`,
  `Медиакит и пресс-контакт — ${AWAITING}.`,
];

// Съёмка на площадке из блока тоже ушла. §6.3 сняла строку «Приём посетителей —
// по договорённости» и кнопку «Записаться на визит», а §6.5 запрещает обещать
// со страниц процессы, которых никто не подтверждал. Приглашение снимать
// производство — то же самое обещание, просто адресованное журналисту.
export const press = {
  eyebrow: "Для СМИ",
  title: "Комментарии и материалы",
  text: `Запросы на комментарии и материалы принимает специалист VEDAL: ${site.phone}, ${site.email}. Пресс-контакт и медиакит — ${AWAITING}.`,
};

export const subscribe = {
  title: "Подписка на релизы",
  text: "Пришлём пресс-релизы и новости о выпуске изделий. Без рекламы и рассылок партнёров.",
  placeholder: "Рабочая почта",
  submit: "Подписаться",
};
