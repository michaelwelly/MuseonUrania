# common

Общее для всех модулей: базовые типы, ошибки API, правила валидации,
работа со временем и идентификаторами.

Правило: сюда попадает только то, что нужно двум и более модулям. Один
потребитель — код остаётся у него.
