# Backend

**Русский** · [English](README.en.md)

Серверная часть VEDAL Portal. Java + Spring.

Состав модулей выведен из [брифа собственника](../docs/architecture/vedal_portal_owner_brief.md):
сайт, CRM и закрытый контур — одна платформа. Стандартные сервисы (почта, облако,
БД) покупаем, уникальную логику пишем сами.

## Состав первого релиза

Бриф определяет первый релиз так: публичный сайт и каталог; backend API, формы и
авторизация; лиды, клиенты, сделки, КП и документы; роли, аудит, бэкапы и базовый
gateway. Плюс два модуля из
[спеки архитектуры](../docs/superpowers/specs/2026-08-06-vedal-portal-architecture-design.md),
которых в исходном списке не было, — `content` и `assistant`. Итого одиннадцать:

| Модуль | Назначение |
| --- | --- |
| [app](app/) | Сборка Spring Boot, конфигурация, точка входа |
| [common](common/) | Общие типы, ошибки, валидация, outbox, лимиты частоты |
| [iam](iam/) | Пользователи, роли, доступ |
| [catalog](catalog/) | Продукция и категории, публичное API для сайта |
| [content](content/) | Новости и пресс-центр |
| [documents](documents/) | Документы, статусы публикации, объектное хранилище |
| [gateway](gateway/) | Приём заявок с сайта: проверка, approve, передача в CRM |
| [crm](crm/) | Заявки, клиенты, сделки, КП, история переписки, аналитика воронки |
| [notifications](notifications/) | Письма клиентам и уведомления менеджерам |
| [assistant](assistant/) | Урания: ответы по опубликованному, жёсткие ограничения |
| [audit](audit/) | Журнал действий и доступа к документам |

Каждый — **модуль Maven** со своим `pom.xml`, своим `src/` и своим README:
`backend/crm/`, `backend/catalog/` и так далее. Собирается всё одним
реактором из `backend/pom.xml`.

Граница модуля держится сборкой, а не дисциплиной. Раньше весь код лежал
одной кучей в `backend/src/`, папки рядом содержали по README, и ничто
не мешало `crm` залезть в потроха `catalog` — заметить это можно было только
на ревью. Теперь такой импорт роняет компиляцию.

Зависимости между модулями — это граф без единого цикла, и он же записан
в `pom.xml`:

```
common          ← фундамент, не зависит ни от кого
iam             ← вход, не зависит ни от кого
audit           ← common
catalog         ← common, audit
content         ← common, audit
documents       ← common, audit
crm             ← common, audit, documents
gateway         ← common, crm
notifications   ← common, crm
assistant       ← common, audit, catalog, content, documents
admin           ← common, audit, catalog, content, crm, documents
app             ← собирает всё в одно приложение
```

Модуль видит у соседа только его интерфейс — `LeadIntake`, `CatalogQuery`,
`DocumentQuery`, `*Admin`. Это то же правило, что было записано в спеке,
но теперь оно проверяется: `gateway` объявляет зависимость от `crm` и всё
равно не может дотянуться до `LeadRepository`, потому что публичный контракт
модуля — это то, что он показывает соседям.

**Единственный модуль, который превращается в приложение, — `app`.** Он
объявляет остальные, держит точку входа, конфигурацию и схему. Контейнер
по-прежнему один: «типа микросервисы, но типа монолит» — граница на уровне
сборки, один процесс на уровне запуска. Транзакционный outbox от этого цел:
строка сущности и строка события коммитятся одним `COMMIT`, потому что база
и транзакция общие.

Отдельно от реактора живёт [api-gateway](api-gateway/) — вот он настоящий
второй сервис со своим контейнером. У него своя пара версий Spring Boot
и Spring Cloud Gateway, единственная поддерживаемая, и общий родительский
pom их бы столкнул.

## Путь заявки

Он же определяет границы модулей. Из брифа:

```
Заявка (сайт, форма, почта)
  → gateway     проверяет источник, поля и вложения
  → crm         создаёт черновик лида без доступа к закрытым данным
  → crm         сделка, КП, документы, статусы внутри портала
  → notifications  наружу уходит только шаблонное письмо
```

Каждый шаг пишет в `audit`.

## Что отложено

Папок пока нет:

- **knowledge** — внутренний AI-поиск по документации, этап 3 в [роадмапе](../docs/operations/roadmap.md).
- **vlm** — визуальные модели для сервиса и производства, отдельный пилот.

Отложено и внутри существующих модулей:

- **Переезд на инфраструктуру Яндекса.** Шаг 8. Упирается в Yandex Cloud,
  которого пока нет: Managed PostgreSQL, Managed Kafka, бэкапы, мониторинг.
  Ловушка: у Managed PostgreSQL логическая репликация включается отдельным
  флагом, без него коннектор Debezium не поднимется.
- **`ETag` на публичном API.** Спека выносит его в следующий этап: на двенадцати
  позициях выигрыш нулевой, `Cache-Control` уже стоит.
- **Второй фактор.** Включается политикой realm'а в развёрнутой среде. Портала
  это не касается: он проверяет выданный токен и не знает, сколько факторов было
  предъявлено при входе.
- **Чистка outbox.** Таблица растёт без предела.

## Инфраструктура: что осталось и на чём стоит

| Что | Состояние | На чём стоит |
| --- | --- | --- |
| Kafka: публикация | ✅ готова. `vedal.events.publisher=kafka` — relay публикует сам, `debezium` — читает коннектор. Топики заводит приложение | — |
| Kafka: потребители | ✅ читают из топиков в режиме `debezium`, повторы отсекаются по `(потребитель, событие)`, необработанное уезжает в `<топик>.dlq` | — |
| Object Storage | ✅ S3 через AWS SDK: локально MinIO, две области — закрытые документы и открытое медиа | Каким будет облако — открытый вопрос в [infrastructure_architecture.md](../docs/architecture/infrastructure_architecture.md) |
| Провайдер идентичности | ✅ Keycloak: портал проверяет токен и разбирает `realm_access.roles`. Локальные учётки — запасной режим `vedal.iam.mode=local` | Кто держит Keycloak в развёрнутой среде |
| Единая точка входа | ✅ [api-gateway](api-gateway/README.md) на Spring Cloud Gateway | — |
| pgvector | Не используется | **В установке PostgreSQL для Windows от EnterpriseDB расширения нет** — `pg_available_extensions` его не знает. Понадобится образ `pgvector/pgvector:pg16`, и переводить на него надо одновременно `compose.yaml` и `PostgresTestBase`, иначе тесты разойдутся с разработкой |
| Managed PostgreSQL, VM, бэкапы, мониторинг | Нет | Yandex Cloud |
| Бэкапы: `wal-g`, недельный `pg_dump`, ежемесячная проверка восстановления | Нет | Облако и сервисные аккаунты. Раздел спеки «Бэкапы и восстановление» описывает целевую схему целиком |

**Разные базы данных архитектурой не предусмотрены.** Одна база — следствие
транзакционного outbox: строка сущности и строка события обязаны коммититься
одним `COMMIT`. В разделе спеки «Рассмотрено и отклонено» это записано прямым
текстом на примере MongoDB. Разные базы здесь бывают только в одном смысле —
по одной на окружение.

### Расхождение, которое надо разрешить

Спека называет брокером **Kafka**, а
[infrastructure_architecture.md](../docs/architecture/infrastructure_architecture.md)
в списке компонентов — **Redis/RabbitMQ для задач индексации**. Документы
противоречат друг друга, решение не зафиксировано. Пока это не разрешено,
`EventPublisher` остаётся портом с реализацией в лог: смена брокера не тронет
ни один домен.

## Что не хранится и не выносится наружу

Из брифа, раздел «Не выносим наружу»: клиентская база, коммерческие условия,
договоры, счета, маржа, токены, персональные данные. Публичное API сайта отдаёт
только каталог и согласованные документы.

## Инфраструктура

Покупаем, а не пишем: Яндекс 360 (почта, календарь), Yandex Cloud (VM, бэкапы,
логи, мониторинг), Managed PostgreSQL, Lockbox, WAF по мере готовности.

## Как запустить

Нужен JDK 25 и Docker. Maven ставить не надо — в репозитории лежит wrapper.

**Весь стек сразу**, включая портал, шлюз и сайт. Так собирается то, что уедет
на сервер, и всё оказывается за единой точкой входа `http://localhost:8080`:

```
docker compose -f backend/compose.yaml --profile app up -d --build
```

Дальше — режим, в котором приложения запускаются с машины и видны в отладчике.

**База и окружение.** Либо контейнерами:

```
docker compose -f backend/compose.yaml up -d
```

либо нативной установкой PostgreSQL 16 на порт 5434 с ролью и базой `vedal`.
Адрес, пользователь и пароль переопределяются переменными `VEDAL_DB_URL`,
`VEDAL_DB_USER`, `VEDAL_DB_PASSWORD`; значения по умолчанию совпадают с
`compose.yaml`. Схему ведёт только Flyway, миграции применяются на старте.

**Приложение.**

```
cd backend
./mvnw spring-boot:run
```

Окружения из спеки — `prod`, `staging`, `internal`, `lab` — лежат в
`application-<профиль>.properties`. В них подключение задаётся **только**
переменными окружения, без dev-значений: прод, молча поднявшийся на базе
разработки, хуже прода, который не поднялся. Проверка идёт до создания
datasource и называет недостающие переменные поимённо:

```
Профиль 'prod' требует переменные окружения, они не заданы:
VEDAL_DB_URL, VEDAL_DB_USER, VEDAL_DB_PASSWORD, VEDAL_PORTAL_URL, VEDAL_STORAGE_ROOT
```

В развёрнутых профилях логи структурные (ECS JSON в stdout) — их собирает
Yandex Cloud Logging. В профиле по умолчанию логи остаются читаемыми глазами.

Первый администратор создаётся, только если заданы обе переменные — иначе
учётной записи просто не будет:

```
VEDAL_ADMIN_USER=editor VEDAL_ADMIN_PASSWORD=<пароль> ./mvnw spring-boot:run
```

Приложение поднимается на `http://localhost:8081`. Порт не 8080, потому что на
машине разработки он занят чужим контейнером.

## Что уже работает

Три двери из спеки: публичное чтение, единственная запись снаружи, админка.

| Маршрут | Кто ходит | Что делает |
| --- | --- | --- |
| `GET /actuator/health` | мониторинг | живость; остальные endpoint'ы закрыты |
| `GET /api/public/v1/categories` | сборка сайта | категории каталога |
| `GET /api/public/v1/products` | сборка сайта | только опубликованные позиции, `Cache-Control: max-age=300` |
| `GET /api/public/v1/products/{slug}` | сборка сайта | карточка изделия; неопубликованное — 404 |
| `GET /api/public/v1/news` | сборка сайта | лента; пусто — это `[]`, а не ошибка |
| `GET /api/public/v1/news/{slug}` | сборка сайта | публикация; неопубликованная — 404 |
| `GET /api/public/v1/documents` | сборка сайта | перечень со статусом доступа; ссылка на файл только у опубликованных |
| `GET /api/public/v1/documents/{slug}/file` | посетитель | файл; закрытый — 404, обращение пишется в журнал |
| `POST /api/forms/v1/leads` | формы сайта | приём заявки, заголовок `Idempotency-Key`, ответ `202`; необязательные `language` и `campaign` — атрибуция для аналитики |
| `POST /api/assistant/v1/ask` | Урания | ответ по опубликованному со ссылками; нет источников — передача человеку |
| `/api/admin/v1/**` | админка | сорок шесть маршрутов, шестьдесят четыре операции: продукция, категории, новости, документы, заявки, клиенты, сделки, КП, история переписки, аналитика воронки, журнал, изображения, сессия |

Публичной двери к CRM нет: клиентская база, суммы сделок и цены КП — закрытый
контур.

Ошибки всех дверей — `application/problem+json` (RFC 9457). У форм и ассистента
свои лимиты частоты по адресу клиента. Контракт админского API —
[docs/api/vedal-admin-openapi.yaml](../docs/api/vedal-admin-openapi.yaml).

Правила публикации живут в доменах (`CatalogEditor`, `ContentEditor`,
`DocumentEditor`, `LeadTriage`), а не в контроллерах: дверей две, и правило,
написанное в контроллере, соблюдается только в одной из них. У CRM то же самое:
`ClientDesk`, `DealDesk`, `QuoteDesk`, `HistoryDesk` и `Pipelines` — правила
воронок, версий и КП, а `AdminClientsApi`, `AdminDealsApi`, `AdminQuotesApi`
и `AdminCrmAnalyticsApi` — только транспорт.

## Что закрыто ограничениями базы, а не кодом

Правила, которые нельзя обойти правкой контроллера или ошибкой редактора:

- `document_public_only` — внутренний и конфиденциальный документ не может стать
  публично скачиваемым;
- `document_published_has_file` — публикация без загруженного файла запрещена;
- `document_published_is_listed` — опубликованный документ обязан быть в перечне;
- `lead_idempotency_key_idx` — повтор отправки формы не создаёт вторую заявку;
- `event_consumed_idx` — повторная доставка события не приводит ко второму письму;
- `news_published_needs_date` — опубликованная новость обязана иметь дату;
- `deal_stage_check` — стадия сделки обязана быть из её воронки;
- `deal_lead_idx` — заявка разбирается в сделку один раз;
- `client_inn_idx` — вторая карточка с тем же ИНН развела бы историю одной
  организации по двум местам;
- `quote_sent_has_date` — отправленное КП обязано помнить, когда его отправили;
- `interaction_has_subject` — запись истории привязана к сделке, клиенту или
  заявке, а не висит в воздухе;
- триггер `audit_entry_append_only` — журнал нельзя править задним числом.

Одно ограничение отложенное — `quote_item_position_unique`. Позиции КП
заменяются целиком, и Hibernate в одном сбросе вставляет новую строку раньше,
чем удаляет старую; проверка на `COMMIT` оставляет правило строгим и снимает
зависимость от порядка операций внутри транзакции.

Триггер не защищает от `TRUNCATE`: statement-триггеры на него не срабатывают.
Настоящая защита — отзыв прав `UPDATE`/`DELETE`/`TRUNCATE` у роли приложения,
это шаг администратора при развёртывании среды.

## Порты наружу

Реализация меняется конфигурацией, домены за порт не заглядывают.

| Порт | Сейчас | Дальше |
| --- | --- | --- |
| `EventPublisher` | `log`, `kafka` или `debezium` по `vedal.events.publisher` | Managed Kafka |
| `MailSender` | запись в лог | SMTP Яндекс 360 |
| `FileStorage` | `local` или `s3` по `vedal.storage.kind`; в стеке MinIO, предел 20 МБ | Yandex Object Storage |
| `LlmEngine` | детерминированный поиск по словам | YandexGPT + pgvector |

Выбор реализации у всех трёх — свойством, а не составом classpath и не
«какой бин нашёлся»: молчаливое переключение хранилища или брокера однажды
означает локальный каталог вместо S3 на проде.

Каталог наполняется миграцией `V2__catalog_seed.sql`, которая **генерируется**,
а не пишется руками:

```
node backend/tools/seed-catalog.mjs > backend/src/main/resources/db/migration/V2__catalog_seed.sql
```

Источник — `frontend/content/products.ts`. Править `V2` вручную бессмысленно:
перегенерировать и закоммитить заново.

## Тесты

```
cd backend
./mvnw test
```

Тесты идут на настоящем PostgreSQL через Testcontainers, поэтому **требуют
запущенного Docker**. H2 не используется: различия диалектов должны вылезать
здесь, а не на проде.

Контейнер даёт `PostgresTestBase`, и наследовать его обязаны **все** тестовые
классы без исключения. Тест без базового класса поднимает контекст на
`spring.datasource.url`, то есть на базе разработки той машины, где его
запустили: прогон молча накатывает миграции на живую базу, правка ещё не
выпущенной миграции ломает сборку расхождением контрольной суммы, а на машине
без локального PostgreSQL тест падает подключением при запущенном Docker
и зелёных соседях.

Проверка покрытия — отдельная фаза, `./mvnw verify`: планка стоит в `pom.xml`
и работает как храповик.

## Принятые решения

| Вопрос | Решение | Почему |
| --- | --- | --- |
| Сборка | Maven + wrapper | Скучный стандарт, wrapper снимает установку Maven с машины |
| Java | 25 | В системе стоит JDK 25, Spring Boot 4.1 её поддерживает |
| Число сервисов | Одно приложение | 50 сотрудников; модули — границы пакетов `ru.vedal.portal.*`, а не отдельные деплои |
| Раскладка кода | Реактор Maven: `backend/<модуль>/src/main/java/ru/vedal/portal/<модуль>/` | Папка модуля содержит его код, его pom и его README. Границу проверяет сборка, а не ревью |
| Число деплоев | Один: собирает `app` | Модули — граница сборки, а не отдельные процессы. Разделить их на сервисы значит потерять один `COMMIT` на сущность и событие, то есть терять заявки |

Spring Boot 4.1.0 на Spring Framework 7. Подключены: webmvc, validation,
actuator, data-jpa, flyway, security, thymeleaf; Jackson 3.

Три ловушки Boot 4, на которые здесь уже наступили:

- **Flyway подключается через `spring-boot-starter-flyway`,** а не через один
  `flyway-core`. Автоконфигурации в Boot 4 разнесены по модулям: с одним
  `flyway-core` миграции не применяются **молча**, без ошибки в логе.
- **`@AutoConfigureMockMvc`** живёт в `org.springframework.boot.webmvc.test.autoconfigure`,
  а не в `org.springframework.boot.test.autoconfigure.web.servlet`.
- **Jackson 3** — пакет `tools.jackson.databind`, а не `com.fasterxml.jackson`.

## Что ещё не решено

- **Какое облако** под Document Vault — вопрос открыт и в
  [infrastructure_architecture.md](../docs/architecture/infrastructure_architecture.md).
  На схеме собственника это «Private Object Storage» внутри закрытого контура.
  Хранилище уже говорит на S3, поэтому переезд меняет адрес и ключи, а не код.
- **Кто держит Keycloak** в развёрнутой среде и как в нём заводятся сотрудники.
- **Срок хранения заявок.** Предложено три года, автоочистки нет. Пока срок
  не подтверждён, заводить её нельзя: удаление по неверному сроку необратимо.

Работа ведётся в ветке `back`.
