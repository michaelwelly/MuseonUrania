<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<groupId>org.springframework.boot</groupId>
		<artifactId>spring-boot-starter-parent</artifactId>
		<version>4.1.0</version>
		<relativePath/> <!-- lookup parent from repository -->
	</parent>
	<groupId>ru.vedal</groupId>
	<artifactId>portal</artifactId>
	<version>0.0.1-SNAPSHOT</version>
	<name>portal</name>
	<description>VEDAL Portal backend</description>

	<properties>
		<java.version>25</java.version>
		<!-- Spring Boot 4.1 не тянет testcontainers-bom, версию держим сами. -->
		<testcontainers.version>1.21.4</testcontainers.version>
		<!-- Ветка 3.x springdoc собрана под Spring Boot 4; 2.x — под Boot 3
		     и в этом проекте не поднимется. Версию держим сами: в BOM
		     Spring Boot springdoc не входит. -->
		<springdoc.version>3.1.0</springdoc.version>
		<!-- Клиент объектного хранилища. В BOM Spring Boot не входит. -->
		<awssdk.version>2.52.1</awssdk.version>
	</properties>

	<dependencyManagement>
		<dependencies>
			<dependency>
				<groupId>org.testcontainers</groupId>
				<artifactId>testcontainers-bom</artifactId>
				<version>${testcontainers.version}</version>
				<type>pom</type>
				<scope>import</scope>
			</dependency>
			<dependency>
				<groupId>software.amazon.awssdk</groupId>
				<artifactId>bom</artifactId>
				<version>${awssdk.version}</version>
				<type>pom</type>
				<scope>import</scope>
			</dependency>
		</dependencies>
	</dependencyManagement>

	<dependencies>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-actuator</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-validation</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-webmvc</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-data-jpa</artifactId>
		</dependency>
		<!-- Именно starter, а не flyway-core: в Boot 4 автоконфигурации разнесены
		     по модулям, и с одним flyway-core миграции молча не применяются. -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-flyway</artifactId>
		</dependency>
		<dependency>
			<groupId>org.flywaydb</groupId>
			<artifactId>flyway-database-postgresql</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-security</artifactId>
		</dependency>
		<!-- Публикация событий из outbox. Включается свойством
		     vedal.events.publisher=kafka; по умолчанию события уходят в лог,
		     и брокер не нужен для работы приложения.

		     Именно стартер Boot, а не голый spring-kafka: автоконфигурации
		     в Boot 4 разнесены по модулям, и без стартера нет ни KafkaTemplate,
		     ни создания топиков — ровно как с flyway-core. -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-kafka</artifactId>
		</dependency>
		<!-- Спецификация OpenAPI и Swagger UI поверх аннотаций контроллеров.
		     Спецификация собирается из кода, а не лежит отдельным файлом:
		     отдельный файл расходится с реальными дверями на первой же правке
		     и врёт тем, кто по нему интегрируется. -->
		<dependency>
			<groupId>org.springdoc</groupId>
			<artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
			<version>${springdoc.version}</version>
		</dependency>
		<!-- Проверка токена Keycloak на дверях админки. Именно resource server,
		     а не client: портал токены не выдаёт и вход не рисует — он их
		     проверяет. Логин живёт в Keycloak, редирект — на стороне админки. -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-oauth2-resource-server</artifactId>
		</dependency>
		<!-- Объектное хранилище. Клиент — AWS SDK, а не родная библиотека MinIO:
		     локально это MinIO, в облаке будет Yandex Object Storage, и оба
		     говорят на S3. С родной библиотекой переезд означал бы переписать
		     клиент, с SDK — поменять адрес и ключи.

		     netty-nio-client выброшен: асинхронный клиент здесь не используется,
		     а тянет он половину Netty. Синхронный ходит через apache-client. -->
		<dependency>
			<groupId>software.amazon.awssdk</groupId>
			<artifactId>s3</artifactId>
			<exclusions>
				<exclusion>
					<groupId>software.amazon.awssdk</groupId>
					<artifactId>netty-nio-client</artifactId>
				</exclusion>
			</exclusions>
		</dependency>
		<dependency>
			<groupId>org.postgresql</groupId>
			<artifactId>postgresql</artifactId>
			<scope>runtime</scope>
		</dependency>

		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-test</artifactId>
			<scope>test</scope>
		</dependency>
		<!-- Именно стартер Boot, а не голый spring-security-test: в Boot 4
		     тестовые автоконфигурации разнесены по модулям, и без него
		     springSecurity() к MockMvc не применяется — @WithMockUser молча
		     не аутентифицирует запрос, а тест с andExpect(is3xxRedirection())
		     проходит на редиректе к форме входа, ничего не проверив. -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-security-test</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-testcontainers</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.testcontainers</groupId>
			<artifactId>postgresql</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-actuator-test</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-validation-test</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-webmvc-test</artifactId>
			<scope>test</scope>
		</dependency>
	</dependencies>

	<build>
		<plugins>
			<plugin>
				<groupId>org.springframework.boot</groupId>
				<artifactId>spring-boot-maven-plugin</artifactId>
			</plugin>
		</plugins>
	</build>

</project>
