# Сценарий показа портала заказчику

**Русский** · [English](demo_script.en.md)

Маршрут на 15–20 минут по живому стенду. Не презентация — по нему ведут
показ, не решая на ходу, что открыть следующим. Источник фактов: протокол
сквозного прогона [issue #57](https://github.com/michaelwelly/MuseonUrania/issues/57)
от 7 сентября (после выкатки `1ec291b`) и
[реестр выполненных работ](../commercial/act2_work_registry.md).

## Перед показом (пять минут)

Снаружи канал до стенда моргает и может отдать таймаут при полностью
здоровом портале (issue #64) — так уже было на прогоне 7 сентября. Поэтому
проверка **изнутри ВМ**, а сам показ ведём с машины, у которой канал ровный
(офис, не мобильный интернет через тот же маршрутизатор, что и утром).

Изнутри ВМ:

```bash
ssh -p 2222 ubuntu@51.250.31.97
curl -s http://127.0.0.1:18080/actuator/health
curl -s -o /dev/null -w '%{http_code}\n' http://127.0.0.1:18080/
```

Первая команда должна вернуть `{"status":"UP"}`, вторая — `200`.

Вход в админку — с машины показа, заранее, до заказчика:

- открыть `http://51.250.31.97:18080/admin/`, войти под `asura`
  (роль `portal-admin`);
- дойти до кабинета и обратно на `/admin/` без ошибок.

Если вход не проходит — см. раздел «Если что-то пойдёт не так» ниже, до
показа, не при заказчике.

## Маршрут показа

### 1. Публичный сайт

Все адреса — `http://51.250.31.97:18080`.

| Шаг | Что открыть | На что обратить внимание | Фраза |
| --- | --- | --- | --- |
| 1.1 | `/` | прелоадер, живой знак VEDAL, анимации | «Это витрина, она статическая — падение бэкенда её не роняет» |
| 1.2 | `/products` | пять категорий, тринадцать карточек | «Каталог собирается из той же базы, что видит редактор в админке» |
| 1.3 | `/products/[slug]` — открыть одну карточку | вкладки на карточке, характеристики | если по конкретной позиции характеристика «уточняется» — не обходить молчанием, сказать прямо: «данные ждём от вас, ничего не придумываем» |
| 1.4 | `/production` | текст о производстве | коротко, без задержки |
| 1.5 | `/documents` | перечень документов | **не нажимать скачивание** — см. раздел «Что не открывать» |
| 1.6 | `/contacts` | карта, реквизиты | не задерживаться на КПП — известно, что неверный (issue #68), ждём значение от заказчика |

### 2. Ведалина

Открыть виджет с любой карточки изделия.

| Шаг | Что сделать | На что обратить внимание | Фраза |
| --- | --- | --- | --- |
| 2.1 | Спросить про опубликованное изделие, например про мониторинг пациента | ответ со ссылками на источники под текстом | «Ссылки даёт портал, а не модель — это не выдумка, это цитата опубликованной страницы» |
| 2.2 | Спросить про то, чего на сайте нет (модель или задача, не описанная в каталоге) | ассистент не выдумывает, уточняет и предлагает передачу человеку | «Нет источника — нет ответа, есть переход к специалисту» |
| 2.3 | Спросить цену | ответ «цены не публикуются, оставьте заявку» — можно спросить и по-английски, ответит на языке вопроса | «Цену Ведалина не называет никогда, это правило, а не забывчивость» |

### 3. Заявка с формы

| Шаг | Что сделать | На что обратить внимание | Фраза |
| --- | --- | --- | --- |
| 3.1 | Заполнить и отправить форму заявки (с любой карточки или из шапки) | подтверждение отправки на сайте | «Заявка ушла с согласием на обработку персональных данных» |

### 4. Админка — разбор заявки

Открыть `/admin/` (уже вошли на этапе подготовки).

| Шаг | Что открыть | На что обратить внимание | Фраза |
| --- | --- | --- | --- |
| 4.1 | `/admin/leads` | новая заявка в списке, тот номер, что появился на шаге 3 | «Заявка попала в CRM сразу, без ручного переноса» |
| 4.2 | открыть заявку, назначить ответственного | статус, история | коротко |
| 4.3 | разобрать заявку в сделку → `/admin/deals` | сделка появилась в нужной воронке | «Дальше заявка живёт как сделка — воронка, стадии, КП» |

### 5. Разговоры

| Шаг | Что открыть | На что обратить внимание | Фраза |
| --- | --- | --- | --- |
| 5.1 | `/admin/chats` | список разговоров, круглая иконка виджета на сайте не перекрывает контент (issue #49 закрыт) | «Сюда попадают обращения из чата, когда посетитель зовёт специалиста» |

### 6. Журнал аудита

| Шаг | Что открыть | На что обратить внимание | Фраза |
| --- | --- | --- | --- |
| 6.1 | `/admin/audit` | фильтры, цепочка по заявке из шага 3 | «Каждое действие в системе — от входа до правки карточки — записано и связано в цепочку» |

На этом маршрут закончен, около 15–20 минут с учётом пауз на вопросы.

## Что не открывать и почему

**Скачивание документов.** На стенде 12 карточек документов, у всех
`published = false` и `storage_key` пуст — файлов физически нет ни у
одного. Это не поломка портала, а отсутствие материалов от заказчика
(issue #35). Кнопку в `/documents` не нажимать.

Если спросят: «Механика загрузки, публикации и выдачи документа написана
и проверена — закрытый документ отдаёт 404 и пишет обращение в журнал
(`document.access.denied`), это в прогоне подтверждено. Не хватает
материалов и разрешения на публикацию — как только они появятся, документы
встанут на витрину тем же путём, каким сегодня встают карточки продукции».

**Письма о заявках.** Уведомление о новой заявке не уходит на почту:
почтовый ящик не настроен, SMTP не подключен (issue #46). Заявка при этом
в базе и видна в `/admin/leads` — показ шага 4 это не затрагивает.

Если спросят: «Письмо не теряется — оно встаёт в очередь и уйдёт, как
только появится почтовый ящик; сама отправка проверена тестом, ждём только
ящик от заказчика».

## Если что-то пойдёт не так

**Страница не открывается или зависает.** Скорее всего канал (issue #64),
не портал. Проверить изнутри ВМ: `ssh -p 2222 ubuntu@51.250.31.97`, дальше
`curl http://127.0.0.1:18080/actuator/health`. Если изнутри всё `UP` —
переключиться на резервный канал (телефон с другим оператором) или
показать те же экраны через SSH-туннель/скриншот, не пытаясь достучаться
снаружи повторно теми же средствами.

**Вход в админку не пускает.** Проверить, той ли ролью заходят: `asura` —
`portal-admin`, у `sales` и `production` доступ уже, по замыслу, урезан
(`/products` и `/audit` отвечают им `403` — это правильное поведение, не
баг). Подробности заведения учёток и известная ловушка Keycloak
(`invalid_grant: Account is not fully set up`, пока не заполнены имя и
фамилия пользователя) — issue
[#43](https://github.com/michaelwelly/MuseonUrania/issues/43).

**Ведалина не отвечает или отвечает шаблонно без источников там, где
источник есть.** Проверить движок и ключ:
[docs/operations/yandexgpt_activation.md](yandexgpt_activation.md) —
переменные `VEDAL_ASSISTANT_ENGINE=yandexgpt`,
`VEDAL_YANDEXGPT_API_KEY`, smoke-test через `curl` на
`/api/assistant/v1/ask` из этого документа.
